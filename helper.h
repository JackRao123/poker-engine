#include <vector>
#ifndef HELPER_H // Include guard to prevent multiple inclusions
#define HELPER_H
void printArray(std::vector<int> arr);
bool contains(std::vector<int> array, int value);

#endif //HELPER_H